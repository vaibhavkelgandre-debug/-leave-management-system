# Leave Management System

Project rules live in [.claude/rules.md](rules.md) — **read that file, not this one.** It is an index: the rules
themselves are in `.claude/rules/`, split by area (architecture, database, email, access control, testing, workflow,
UI components). Read the file covering whatever you are about to touch, plus
[rules/10-workflow-and-documentation.md](rules/10-workflow-and-documentation.md) and
[rules/13-claude-rules.md](rules/13-claude-rules.md), which apply to every change.

The five rules that govern everything are stated inline at the top of the index, so start there.
