# Leave Management System

Project rules live in [.claude/rules.md](rules.md) — **read that file, not this one.** It is an index: the rules
themselves are in `.claude/rules/`, five files split by area. Read the one covering whatever you are about to touch,
plus [rules/04-workflow-deployment-and-claude.md](rules/04-workflow-deployment-and-claude.md), which applies to every
change.

The five rules that govern everything are stated inline at the top of the index, so start there.
